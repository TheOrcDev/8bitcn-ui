# Implementation Plans

Generated by the improve skill on 2026-08-05. Execute plans in the order
listed below. Each executor must read the complete plan before starting, honor
its STOP conditions, and update the status row when finished.

## Execution order and status

| Plan | Title | Priority | Effort | Depends on | Status |
| --- | --- | --- | --- | --- | --- |
| [001](./001-optimize-homepage-cold-path.md) | Make the homepage cold path independent of showcase, sponsor effects, fonts, and GitHub | P1 | L | — | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale)

## Dependency notes

- Plan 001 is intentionally one ordered plan because every later optimization
  uses the benchmark harness created in its first step. Do not split the code
  changes into parallel branches; their performance results would not be
  comparable.

## Findings considered and rejected

- Removing Wandry analytics from `proxy.ts`: rejected because diagnosis measured
  only 88–146 ms on cold routes and about 2 ms warm. It is not the dominant
  first-response cost.
- Removing the component showcase or sponsor section: rejected because these
  are product content. The plan preserves their output and defers only their
  client code.
- Treating the 5.12-second metric as production TTFB: rejected. The benchmark
  deletes `.next` and measures a cold `next dev` route compilation; production
  request latency was not measured.
